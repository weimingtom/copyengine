package copyengine.scenes.isometric
{
	/**
	 *IsoFloor is each Floor union data. 
	 * in Copyengine not separate each tile to independent class ,
	 * inside use some uint to recoder each tile attribute.
	 * 		ex: is current tile block , is current can put flower etc
	 * 
	 * each level(same Z value) should have one IsoFloor class for render the floor
	 *  
	 * @author Tunied
	 * 
	 */	
	public final class IsoFloor
	{
		public function IsoFloor()
		{
		}
		
	}
}